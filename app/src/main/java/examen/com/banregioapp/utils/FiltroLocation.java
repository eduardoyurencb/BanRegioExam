package examen.com.banregioapp.utils;

import android.location.Location;

import java.util.ArrayList;
import java.util.Collections;

import examen.com.corebanregio.model.BranchResponse;
import examen.com.corebanregio.utils.Filter;

public class FiltroLocation {
    /*static ArrayList<BranchResponse> arrayList(
            Location location,
            ArrayList<BranchResponse> arrayList
    ){
        for (BranchResponse item :  arrayList) {
            item.setDistanceBeetwenUser(Filter.Companion.getDistance(location, item));
        }

        return Collections.sort(arrayList);
    }*/
}
